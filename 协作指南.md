# Bilitro 团队协作指南

> 面向组员的 Git / GitHub 协作流程。跟着做一遍就会了。
> 有问题随时在群里问，或者直接问 Kimi。

---

## 一、一次性准备

### 1. 安装 Git

- 下载：https://git-scm.com/download/win
- 安装时一路默认即可。
- 装完后右键任意文件夹应能看到 `Git Bash Here`。

### 2. 配置身份（只需要一次）

打开 Git Bash，执行（换成你自己的名字和 GitHub 注册邮箱）：

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

### 3. 接受协作者邀请

- 让仓库主人（Matanaiz）在仓库页 `Settings → Collaborators → Add people` 添加你的 GitHub 用户名。
- 你会收到一封邀请邮件（或在 https://github.com/notifications 看到），点 **Accept invitation**。
- ⚠️ 我们是小组协作，**不需要 Fork**，直接作为协作者开发即可。

### 4. 克隆项目到本地

找一个你想放项目的目录，右键 → `Git Bash Here`：

```bash
git clone https://github.com/Matanaiz/bilitro.git
cd bilitro
```

之后用 IDEA 打开这个 `bilitro` 文件夹即可（Maven 项目，等它自动下载依赖）。

---

## 二、日常开发流程（最重要的三步）

```
开工前：  git pull          ← 先拉取别人的最新改动
写代码：  ...正常写代码...
收工时：  git add .         ← 暂存改动
         git commit -m "说明"  ← 提交到本地
         git push           ← 推送到 GitHub
```

一句话：**开工先 pull，收工就 commit + push。**

### 详细说明

```bash
# 1. 开工前，同步远程最新代码
git pull

# 2. 写代码（在 IDEA 里正常改）

# 3. 查看自己改了哪些文件
git status

# 4. 暂存全部改动
git add .

# 5. 提交，说明写清楚做了什么
git commit -m "实现 GameSession 出牌逻辑"

# 6. 推送到 GitHub
git push
```

commit message 写清楚干了什么，例如：`修复弃牌次数不刷新的 bug`、`完成商店界面布局`。

---

## 三、常见问题

### 1. push 被拒绝（rejected / fetch first）

说明别人在你之前推了改动。先拉取合并，再重新推送：

```bash
git pull
git push
```

### 2. pull 之后出现冲突（CONFLICT）

说明你和别人改了**同一文件的同一处**。打开冲突文件，会看到：

```
<<<<<<< HEAD
你写的内容
=======
别人写的内容
>>>>>>> origin/main
```

手动改成最终想要的内容，**删掉 `<<<<<<<`、`=======`、`>>>>>>>` 三行标记**，保存后：

```bash
git add .
git commit -m "解决 xxx 文件的冲突"
git push
```

拿不准就先把两段都发给对方，在群里确认保留哪段。

### 3. 本地有未提交的改动，pull 报错

先提交自己的改动，再 pull：

```bash
git add .
git commit -m "改到一半的内容"
git pull
```

### 4. 改坏了想放弃本地改动（还没 commit）

```bash
git checkout -- 文件名     # 放弃单个文件的改动
git checkout -- .          # 放弃所有未提交改动（慎用）
```

### 5. 查看提交历史

```bash
git log --oneline -10     # 最近 10 条提交
```

---

## 四、约定俗成的规矩

1. **每天开工第一件事：`git pull`**
2. **不要提交编译产物和 IDE 配置**：`target/`、`.idea/` 已被 `.gitignore` 排除，不用管。
3. **按模块分工**，减少冲突：

   | 模块 | 目录 | 负责人 |
   |---|---|---|
   | 模型层 | `src/main/java/com/bilitro/model/` | （待分配） |
   | 控制层 | `src/main/java/com/bilitro/controller/` | （待分配） |
   | 视图层 | `src/main/java/com/bilitro/view/` | （待分配） |

4. **改动大的功能先做完再 push**，别把编译不过的半成品推上去。
5. push 前最好在 IDEA 里确认项目能编译（右上角锤子图标 Build）。

---

## 五、IDEA 图形化操作（不想敲命令的话）

| 操作 | IDEA 位置 |
|---|---|
| pull | 右上角蓝色下拉箭头（Update Project），或 `Ctrl+T` |
| commit | 右上角绿色对勾（Commit），或 `Ctrl+K` |
| push | `Ctrl+Shift+K`，或 commit 窗口里的 `Commit and Push` |
| 解决冲突 | pull 出冲突时自动弹出合并窗口，逐条选「用你的 / 用他的 / 都保留」 |

---

## 六、项目结构速览

```
com.bilitro
├── App.java            # 入口
├── controller/         # 控制层：GameController / MenuController / ShopController
├── model/              # 模型层：card / hand / game / player / shop
└── view/               # 视图层（JavaFX）：GameView / ShopView / SettlementView
```

详见仓库根目录的《项目实现思路.md》。
