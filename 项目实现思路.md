# Bilitro 项目实现思路

> 版本：v1.0（对应需求规格说明书 v1.2）
> 本文档说明项目的整体架构、模块职责、实现顺序与待确认事项。

---

## 一、架构总览：三层 MVC

项目**只保留三个顶层包**，严格遵循组内约定：

| 层 | 包 | 职责 | 约束 |
|---|---|---|---|
| View | `com.bilitro.view` | 只管"长什么样"：界面绘制、动画、音效播放 | **JavaFX 代码只允许出现在这一层** |
| Controller | `com.bilitro.controller` | 只管"用户做了什么"：接收事件 → 调用 model → 通知 view 刷新 | 不写业务规则，不碰 JavaFX 控件细节 |
| Model | `com.bilitro.model` | 只管"游戏规则和数据"：牌组、牌型、计分、商店、存档数据 | **纯 Java，零 JavaFX 依赖，可脱离界面单独单元测试** |

入口类 `com.bilitro.App`（JavaFX Application）：负责装配三层、加载存档、切换场景。

### 调用方向（单向）

```
用户操作 → View（控件事件）→ Controller（翻译为用户意图）→ Model（执行规则、变更数据）
                                                              ↓
用户看到 ← View.renderXxx() ← Controller（把新状态交给 View）←┘
```

Model **不主动调用** View；状态变化统一由 Controller 中转（后续如需可加观察者/事件总线，见"待确认"）。

---

## 二、包结构与文件清单

```
com.bilitro
├── App.java                        # 入口：装配 MVC、检测存档、进主菜单
│
├── model/                          # 【模型层：纯 Java】
│   ├── GameConfig.java             # 全局数值常量（手牌数、次数、栏位上限……待定项集中在 TODO）
│   ├── SaveManager.java            # 自动存档(2.2.7) + 最高分记录(2.2.4) 接口
│   ├── card/
│   │   ├── Suit.java               # 花色枚举
│   │   ├── Rank.java               # 点数枚举
│   │   ├── Card.java               # 扑克牌（不可变 record）
│   │   └── Deck.java               # 牌组接口：随机抽牌 draw / 剩余查看 peekRemaining / 按禁用花色重置
│   ├── hand/
│   │   ├── HandType.java           # 牌型枚举 + 优先级
│   │   ├── HandTypeEvaluator.java  # 牌型查表判定 + 出牌合法性校验（决定出牌按钮亮/灰）
│   │   └── ScoreCalculator.java    # 得分 = 牌型基础分 × 倍数 + 功能牌加成
│   ├── player/
│   │   ├── Player.java             # 货币 + 功能牌栏
│   │   └── SpecialCard.java        # 功能牌：id / 描述 / 价格（效果以配置表存储）
│   ├── game/
│   │   ├── GameSession.java        # ★核心：一局的状态（关卡/目标分/剩余次数/手牌）+ play/discard/通关判定
│   │   ├── LevelRule.java          # 关卡差异化规则（目标分、禁用花色、提示文案）
│   │   └── GameState.java          # 状态机：主菜单/回合内/过关/商店/失败/通关
│   └── shop/
│       └── Shop.java               # 随机商品、刷新、购买校验（栏位满→货币不足→成功）
│
├── controller/                     # 【控制层】
│   ├── GameController.java         # 选牌/出牌/弃牌/查看牌组/查看功能牌
│   ├── ShopController.java         # 购买/刷新/离开商店
│   └── MenuController.java         # 新一局/继续游戏/再来一局/设置
│
└── view/                           # 【视图层：JavaFX 全部在此】
    ├── GameView.java               # 对局界面（手牌区、状态区、按钮亮灰、计分特效、说明浮层）
    ├── ShopView.java               # 商店界面
    ├── SettlementView.java         # 结算界面（胜利/失败共用）
    ├── AudioManager.java           # 音效/BGM 开关与播放（实现依赖 javafx.media，故归 view）
    └── components/                 # 复用 UI 组件（卡牌控件 CardNode、按钮等，待建）
```

### 关键设计点（与需求条目映射）

- **出牌/弃牌按钮亮灰（2.1.1）**：每次选中变化时，Controller 调 `HandTypeEvaluator.isPlayable()` 和检查剩余弃牌次数，再调 `GameView.setActionEnabled(canPlay, canDiscard)`。
- **自动计分（2.1.2）**：`GameSession.play()` 内部串联 `HandTypeEvaluator → ScoreCalculator → 扣减剩余目标分 → Deck 补牌`，返回 `PlayResult(牌型, 得分)` 给 View 做数字跳动/特效。
- **通关/失败判定（2.1.4）**：每次操作后 Controller 查询 `GameSession.outcome()`：`LEVEL_CLEARED` → 进商店或下一关；`FAILED` → 结算界面；`ONGOING` → 继续。
- **商店购买（2.2.1）**：`Shop.buy()` 返回枚举 `BuyResult{SUCCESS, SLOTS_FULL, NOT_ENOUGH_COINS}`，View 按枚举弹对应提示，与验收标准逐条对应。
- **随机刷新（2.2.2）**：`Shop.goods()` 在进入商店/刷新时从功能牌池随机抽取。
- **关卡差异化（2.2.9）**：`LevelRule(targetScore, bannedSuits, hintText)` 随关卡配置，`Deck.reset(bannedSuits)` 保证生成与补牌不含禁用花色。
- **存档（2.2.7）**：每次状态变更后由 Model 层序列化 `GameSession`；启动时 App 检测存档决定显示"继续游戏"。
- **最高分（2.2.4）**：`SaveManager.loadHighScore/saveHighScore`，结算时比较出 `newRecord` 标记交给 `SettlementView`。
- **P2 特效/动画**：全部在 View 层，Model 只传数值（如 `playScoreEffect(score)`，特效强度随得分量级）。

---

## 三、实现顺序建议（按优先级）

1. **P0 内核（纯 Model，可先无界面跑单测）**：Card/Deck → HandTypeEvaluator → ScoreCalculator → GameSession
2. **P0 界面**：GameView + GameController，跑通"选牌→出牌→计分→补牌→通关/失败"主循环
3. **P1**：商店（Shop + ShopView + ShopController）→ 再来一局 → 最高分 → 功能牌/牌组查看 → 自动存档 → 音效开关 → 关卡差异规则
4. **P2**：皮肤、计分特效、失败/胜利动画

---

## 四、待确认事项（设定不清楚的地方）

> 以下均已在对应代码文件用 `TODO` 标注，**在实现前需要小组拍板**。

### 数值类（需求文档标注为"示例"）
1. 手牌数量：5 张？   *初始为8张，不固定，有特殊功能牌会修改手牌数量
2. 出牌选中张数范围：1~5？ *是
3. 每关初始出牌次数 / 弃牌次数：各多少？中途是否有补充机制？ *初始都是4次，有关卡限制或特殊功能牌会修改次数
4. 功能牌栏位上限：5？ *6张
5. 初始货币、每关通关奖励货币数额 *每关通关奖励4代币，每剩余1出牌次数奖励1代币，上回合每剩余5代币获得1代币利息

### 规则类
6. **牌型优先级表与基础分/倍数表**：需求明确"由开发组自行定义"——需要一张完整的表（牌型集合、优先级、基础分、倍数），决定 `HandType` 枚举和 `ScoreCalculator` 的数据来源（硬编码 or 配置文件）
7. 点数大小规则：A 算最大还是 1？顺子是否允许 A-2-3-4-5？*A算最大，不允许
8. 是否需要大小王 / 特殊牌 *否
9. 关卡差异化规则除"禁用花色"外还有哪些？是否需要多条规则叠加（则 `LevelRule` 需改为规则列表）*稍后具体再添加
10. 共多少关？目标分如何递增？ *暂定5-8关，目标分数待定

### 功能牌
11. 效果实现方式：枚举硬编码 / 策略类 / 配置表驱动？（需求假设 7 说"以配置表存储"，配置表字段需设计）
12. 升级机制：能否升级？升级费用与数值成长规则？*否
13. 功能牌池内容清单 

### 技术类
14. 存档格式：JSON（需引入 JSON 库）还是 Java 序列化？存档文件放哪？
15. 音效/BGM 资源格式与目录约定；开关状态与存档是否同文件持久化
16. 皮肤（P2）资源组织方式与装备数据存哪
17. Model → View 的通知方式：目前设计为 Controller 中转；若状态变多是否引入观察者模式
18. 界面技术选型：纯 JavaFX 代码布局 还是 FXML + Scene Builder？（影响 View 层实现方式）
19. 性能指标 60FPS 的验证方式（是否需要帧率显示调试开关）
